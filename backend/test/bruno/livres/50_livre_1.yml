info:
  name: 50_livre_1
  type: http
  seq: 5

http:
  method: GET
  url: http://{{BRUNO_TEST_URL}}:{{BRUNO_TEST_PORT}}/api/v1/livres/1
  body:
    type: json
    data: |-
      {
        "disponible": false
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("should return livre 1 info", function () {
          const body = res.getBody();
          
          expect(body).to.have.property("id");
          expect(body.titre).to.equal("Clean Code");
          expect(body.genre).to.contain("Informatique");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
