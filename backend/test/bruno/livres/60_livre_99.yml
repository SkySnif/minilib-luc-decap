info:
  name: 60_livre_99
  type: http
  seq: 6

http:
  method: GET
  url: http://{{BRUNO_TEST_URL}}:{{BRUNO_TEST_PORT}}/api/v1/livres/99
  body:
    type: json
    data: ""
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("should return 404", function () {
          expect(res.getStatus()).to.equal(404);
        });

        test("should return livre 99 not found", function () {
          const body = res.getBody();
          
          expect(body.message).to.equal("Livre id:99 not found");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
