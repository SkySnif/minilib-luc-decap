info:
  name: 10_health
  type: http
  seq: 1

http:
  method: GET
  url: http://{{BRUNO_TEST_URL}}:{{BRUNO_TEST_PORT}}/health
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("should return 200", function () {
          expect(res.getStatus()).to.equal(200);
        });

        test("should return livre 99 not found", function () {
          const body = res.getBody();

          expect(body.message).to.equal("MiniLib Server up");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
