info:
  name: livres disponible
  type: http
  seq: 3

http:
  method: GET
  url: http://{{BRUNO_TEST_URL}}:{{BRUNO_TEST_PORT}}/api/v1/livres?disponible=true
  params:
    - name: disponible
      value: "true"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
