info:
  name: 30_list_livres_disponibles
  type: http
  seq: 3

http:
  method: GET
  url: http://{{BRUNO_TEST_URL}}:{{BRUNO_TEST_PORT}}/api/v1/livres?disponible=true
  params:
    - name: disponible
      value: "true"
      type: query
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("should return 200", function () {
          expect(res.getStatus()).to.equal(200);
        });

        test("should return array of livre", function () {
          const adherents = res.getBody();
          expect(adherents).to.be.an("array");
          
          //expect(users).to.have.lengthOf(3);
          
           expect(adherents[0]).to.have.property("id");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
