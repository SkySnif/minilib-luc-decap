info:
  name: 40_delete_adherent
  type: http
  seq: 4

http:
  method: DELETE
  url: http://{{BRUNO_TEST_URL}}:{{BRUNO_TEST_PORT}}/api/v1/adherents/1
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("should return 200", function () {
          expect(res.getStatus()).to.equal(200);
        });

        test("should return new adherant id equal 1 ", function () {
          const body = res.getBody();
          
          expect(body).to.have.property("id");
          expect(body.id).equal(1);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
