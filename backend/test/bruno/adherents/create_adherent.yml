info:
  name: create_adherent
  type: http
  seq: 3

http:
  method: POST
  url: http://{{BRUNO_TEST_URL}}:{{BRUNO_TEST_PORT}}/api/v1/adherents
  body:
    type: json
    data: |-
      {
      "nom": "Luc",
      "prenom": "DECAP",
      "email": "decap.professional@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
