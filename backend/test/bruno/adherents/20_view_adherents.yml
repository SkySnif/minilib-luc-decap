info:
  name: 20_view_adherents
  type: http
  seq: 2

http:
  method: GET
  url: http://{{BRUNO_TEST_URL}}:{{BRUNO_TEST_PORT}}/api/v1/adherents
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("should return 200", function () {
          expect(res.getStatus()).to.equal(200);
        });

        test("should return array of adherents", function () {
          const adherents = res.getBody();
          expect(adherents).to.be.an("array");
          
          //expect(users).to.have.lengthOf(3);
          
           expect(adherents[0]).to.have.property("id");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
